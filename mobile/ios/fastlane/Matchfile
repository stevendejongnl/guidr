git_url("https://git.madebysteven.nl/stevendejong/guidr-certificates.git")
# Git credentials will be configured via git credential helper in the workflow
# This avoids hardcoding credentials and works reliably with Gitea

storage_mode("git")
type("appstore")

app_identifier(["com.guidr"])
username(ENV['APPLE_ID'])
team_id(ENV['APPLE_TEAM_ID'])

# Certificate repo branch
git_branch("main")

# Shallow clone for faster operations
shallow_clone(true)

# Verbose logging for debugging
verbose(true)
